services:
  autodocs:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: autodocs
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    environment:
      - ENV=development
    restart: unless-stopped
