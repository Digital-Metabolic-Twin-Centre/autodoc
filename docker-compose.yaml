services:
  autodocs:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: autodocs
    ports:
      - "8000:8000"
    volumes:
      - ./files:/app/files
      - ./log:/app/log
    environment:
      - ENV=development
    restart: unless-stopped