# Architecture Repo Fixture

Sample repository layout used by architecture documentation generation tests.
It contains a Python entry point, a router, a service, a database model, and a
dependency manifest so the architecture scanner has representative evidence
for every layered section (entry points, routers, services, database models,
API endpoints, and data flow).
